{{- if .Values.digitalTwinRegistry.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "basyx-digitalTwinRegistry.fullname" . }}-config
  labels:
    {{- include "basyx-digitalTwinRegistry.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.environment }}
  {{ $key }}: {{ tpl (print $value) $ | quote }}
  {{- end }}
{{- end }}