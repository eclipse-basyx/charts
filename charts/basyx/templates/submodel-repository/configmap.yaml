{{- if .Values.submodelRepository.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "basyx-submodelRepository.fullname" . }}-config
  labels:
    {{- include "basyx-submodelRepository.labels" . | nindent 4 }}
{{- $env := .Values.submodelRepository.environment | default dict }}
{{- if $env }}
data:
  {{- range $key, $value := $env }}
  {{ $key }}: {{ tpl (print $value) $ | quote }}
  {{- end }}
{{- else }}
data: {}
{{- end }}
{{- end }}
