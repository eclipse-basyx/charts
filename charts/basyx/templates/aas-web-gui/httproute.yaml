{{- if .Values.aasWebGui.enabled }}
{{- if .Values.aasWebGui.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "basyx-aasWebGui.fullname" . }}
  labels:
    {{- include "basyx-aasWebGui.labels" . | nindent 4 }}
  {{- with .Values.aasWebGui.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "common.httpRoute.parentRefs" (dict "root" $ "parentRefs" .Values.aasWebGui.httpRoute.parentRefs) | nindent 4 }}
  hostnames:
    {{- range .Values.aasWebGui.httpRoute.hosts }}
    - {{ tpl .host $ | quote }}
    {{- end }}
  rules:
    {{- range .Values.aasWebGui.httpRoute.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: {{ .pathType }}
            value: {{ tpl .path $ | quote }}
      backendRefs:
        - name: {{ include "basyx-aasWebGui.fullname" $ }}
          port: {{ $.Values.aasWebGui.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
{{- end }}
