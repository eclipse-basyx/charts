{{- if .Values.keycloak.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "basyx-keycloakInit.configName" . }}

data:
  user-profile.json: |
{{ .Values.keycloak.initialization.userProfile | default (dict) | toJson | indent 4 }}

  roles.json: |
{{ concat (.Values.keycloak.initialization.roles | default (list)) (.Values.keycloak.initialization.extraRoles | default (list)) | toJson | indent 4 }}

  clients.json: |
{{ tpl (concat (.Values.keycloak.initialization.clients | default (list)) (.Values.keycloak.initialization.extraClients | default (list)) | toJson) . | indent 4 }}

  users.json: |
{{ concat (.Values.keycloak.initialization.users | default (list)) (.Values.keycloak.initialization.extraUsers | default (list)) | toJson | indent 4 }}
{{- end }}
