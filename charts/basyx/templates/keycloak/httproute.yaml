{{- if .Values.keycloak.enabled }}
{{- if .Values.keycloak.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "basyx-keycloak.fullname" . }}
  labels:
    {{- include "basyx-keycloak.labels" . | nindent 4 }}
  {{- with .Values.keycloak.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "common.httpRoute.parentRefs" (dict "root" $ "parentRefs" .Values.keycloak.httpRoute.parentRefs) | nindent 4 }}
  hostnames:
    {{- range .Values.keycloak.httpRoute.hosts }}
    - {{ tpl .host $ | quote }}
    {{- end }}
  rules:
    {{- range .Values.keycloak.httpRoute.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: {{ .pathType }}
            value: {{ tpl .path $ | quote }}
      backendRefs:
        - name: {{ include "basyx-keycloak.fullname" $ }}
          port: {{ $.Values.keycloak.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
{{- end }}
