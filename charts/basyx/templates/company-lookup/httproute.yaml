{{- if .Values.companyLookup.enabled -}}
{{- if .Values.companyLookup.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "basyx-companyLookup.fullname" . }}
  labels:
    {{- include "basyx-companyLookup.labels" . | nindent 4 }}
  {{- with .Values.companyLookup.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "common.httpRoute.parentRefs" (dict "root" $ "parentRefs" .Values.companyLookup.httpRoute.parentRefs) | nindent 4 }}
  hostnames:
    {{- range .Values.companyLookup.httpRoute.hosts }}
    - {{ tpl .host $ | quote }}
    {{- end }}
  rules:
    {{- range .Values.companyLookup.httpRoute.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: {{ .pathType }}
            value: {{ tpl .path $ | quote }}
      backendRefs:
        - name: {{ include "basyx-companyLookup.fullname" $ }}
          port: {{ $.Values.companyLookup.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
{{- end }}
