{{- if .Values.aasRegistry.enabled -}}
{{- if .Values.aasRegistry.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "basyx-aasRegistry.fullname" . }}
  labels:
    {{- include "basyx-aasRegistry.labels" . | nindent 4 }}
  {{- with .Values.aasRegistry.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "common.httpRoute.parentRefs" (dict "root" $ "parentRefs" .Values.aasRegistry.httpRoute.parentRefs) | nindent 4 }}
  hostnames:
    {{- range .Values.aasRegistry.httpRoute.hosts }}
    - {{ tpl .host $ | quote }}
    {{- end }}
  rules:
    {{- range .Values.aasRegistry.httpRoute.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: {{ .pathType }}
            value: {{ tpl .path $ | quote }}
      backendRefs:
        - name: {{ include "basyx-aasRegistry.fullname" $ }}
          port: {{ $.Values.aasRegistry.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
{{- end }}
