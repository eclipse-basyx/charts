{{- if .Values.dppApi.enabled -}}
{{- if .Values.dppApi.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "basyx-dppApi.fullname" . }}
  labels:
    {{- include "basyx-dppApi.labels" . | nindent 4 }}
  {{- with .Values.dppApi.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "common.httpRoute.parentRefs" (dict "root" $ "parentRefs" .Values.dppApi.httpRoute.parentRefs) | nindent 4 }}
  hostnames:
    {{- range .Values.dppApi.httpRoute.hosts }}
    - {{ tpl .host $ | quote }}
    {{- end }}
  rules:
    {{- range .Values.dppApi.httpRoute.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: {{ .pathType }}
            value: {{ tpl .path $ | quote }}
      backendRefs:
        - name: {{ include "basyx-dppApi.fullname" $ }}
          port: {{ $.Values.dppApi.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
{{- end }}
