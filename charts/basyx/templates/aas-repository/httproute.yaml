{{- if .Values.aasRepository.enabled -}}
{{- if .Values.aasRepository.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "basyx-aasRepository.fullname" . }}
  labels:
    {{- include "basyx-aasRepository.labels" . | nindent 4 }}
  {{- with .Values.aasRepository.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "common.httpRoute.parentRefs" (dict "root" $ "parentRefs" .Values.aasRepository.httpRoute.parentRefs) | nindent 4 }}
  hostnames:
    {{- range .Values.aasRepository.httpRoute.hosts }}
    - {{ tpl .host $ | quote }}
    {{- end }}
  rules:
    {{- range .Values.aasRepository.httpRoute.hosts }}
    {{- range .paths }}
    - matches:
        - path:
            type: {{ .pathType }}
            value: {{ tpl .path $ | quote }}
      backendRefs:
        - name: {{ include "basyx-aasRepository.fullname" $ }}
          port: {{ $.Values.aasRepository.service.port }}
    {{- end }}
    {{- end }}
{{- end }}
{{- end }}
